apply plugin: 'groovy'
apply plugin: 'maven'
apply plugin: 'signing'
description = """Gradle plugin for developing building and building
                   packages for rJava, the interface for using Java with R, a
                   statistical programming language."""

project.targetCompatibility = 1.6
project.sourceCompatibility = 1.6




dependencies {
    compile gradleApi()
    groovy localGroovy()
<<<<<<< HEAD
    compile project(":rjava-annotations")
=======
    compile project(":jannotater-rjava")
>>>>>>> b6b581701be0ddfe7be362c5f26c91aa019e22d8
    groovy 'com.jfolson:gradle-plugin-r:0.1'
    runtime 'com.jfolson:gradle-plugin-r:0.1'
}

repositories {
    mavenCentral()
    mavenLocal()
}

task javadocJar(type: Jar, dependsOn: groovydoc) {
    classifier = 'javadoc'
    from 'build/docs/groovydoc'
}
 
task sourcesJar(type: Jar) {
    from sourceSets.main.allSource
    classifier = 'sources'
}

artifacts {
    archives jar
    archives javadocJar
    archives sourcesJar
}

signing {
    sign configurations.archives
}

repositories {
    mavenCentral()
    mavenLocal()
}

uploadArchives {
    repositories {
        mavenDeployer {
            beforeDeployment { MavenDeployment deployment -> signPom(deployment) }
 
            repository(url: "https://oss.sonatype.org/service/local/staging/deploy/maven2") {
              authentication(userName: sonatypeUsername, password: sonatypePassword)
            }
 
            pom.project {
               name project.name
               packaging 'jar'
               description project.description
               url 'http://github.com/jamiefolson/JAnnotateR/' 
 
               scm {
                   url 'scm:git@github.com:jamiefolson/JAnnotateR.git'
                   connection 'scm:git@github.com:jamiefolson/JAnnotateR.git'
                   developerConnection 'scm:git@github.com:jamiefolson/JAnnotateR.git'
               }
 
               licenses {
                   license {
                       name 'The Apache Software License, Version 2.0'
                       url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                       distribution 'repo'
                   }
               }
 
               developers {
                   developer {
                       id 'jamiefolson'
                       name 'Jamie F Olson'
                   }
               }
           }
        }
    }
}

